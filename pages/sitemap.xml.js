export default function Sitemap(){return null;}export async function getServerSideProps({res}){const raw=process.env.NEXT_PUBLIC_SITE_URL?.replace(/\/$/,'');const base=raw?.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');res.setHeader('Content-Type','application/xml');res.write('<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">'+(base?['/','/about'].map(p=>`<url><loc>${base}${p}</loc></url>`).join(''):'')+'</urlset>');res.end();return {props:{}};}
